- size ||= :mini
- itemprop ||= nil
- if image_url = image.try(:url, size)
  - if image_alt = image.try(:alt)
    = image_tag image_url, alt: image_alt, itemprop: itemprop
  - else
    = image_tag image_url, itemprop: itemprop
- else
  %span{class: "image-placeholder #{size}"}
