- if !@product.taxons.blank?
  #taxon-crumbs
    %h6.product-section-title= t('spree.look_for_similar_items')
    %ul#similar_items_by_taxon
      - @product.taxons.each do |taxon|
        %li= link_to taxon.name, seo_url(taxon)
